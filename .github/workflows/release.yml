name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  validate:
    name: Validate release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Check tag matches Cargo version
        id: version
        run: |
          version="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')"
          tag="${GITHUB_REF_NAME#v}"
          if [ "$version" != "$tag" ]; then
            echo "Cargo.toml version ($version) does not match tag ($tag)" >&2
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Test
        run: cargo test --locked

      - name: Check package
        run: cargo publish --locked --dry-run

  publish-cargo:
    name: Publish to crates.io
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Publish crate
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          name="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].name')"
          version="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')"
          if curl -fsS "https://crates.io/api/v1/crates/${name}/${version}" >/dev/null; then
            echo "${name} ${version} is already published on crates.io"
            exit 0
          fi
          cargo publish --locked

  build-artifacts:
    name: Build release artifacts
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-deb
        run: cargo install cargo-deb --locked

      - name: Build binary
        run: cargo build --release --locked

      - name: Package binary tarball
        run: |
          mkdir -p dist
          cp target/release/pr-manager dist/pr-manager
          tar -C dist -czf "pr-manager-${{ needs.validate.outputs.version }}-x86_64-unknown-linux-gnu.tar.gz" pr-manager

      - name: Build .deb
        run: cargo deb --no-build

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-artifacts
          path: |
            pr-manager-*.tar.gz
            target/debian/*.deb

  publish-apt:
    name: Publish to APT repository
    needs: build-artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-artifacts
          path: ./artifacts

      - name: Check out apt-repo branch
        uses: actions/checkout@v4
        with:
          ref: apt-repo
          path: apt-repo

      - name: Import GPG key
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | base64 -d | gpg --batch --import

      - name: Install reprepro
        run: sudo apt-get update && sudo apt-get install -y reprepro

      - name: Add .deb to repository
        run: |
          cd apt-repo
          find ../artifacts -name '*.deb' -print0 | while IFS= read -r -d '' deb; do
            reprepro remove stable pr-manager || true
            reprepro includedeb stable "$deb"
          done

      - name: Push updated repository
        run: |
          cd apt-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "APT repository already up to date"
            exit 0
          fi
          git commit -m "Update repository: ${{ github.ref_name }}"
          git push

  github-release:
    name: Create GitHub Release
    needs:
      - validate
      - build-artifacts
      - publish-cargo
      - publish-apt
    runs-on: ubuntu-latest
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-artifacts
          path: ./artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ./artifacts/**/*.deb
            ./artifacts/*.tar.gz
          generate_release_notes: true
